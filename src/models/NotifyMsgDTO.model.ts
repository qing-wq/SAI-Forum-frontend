/**
 * 消息Id
 */
type msgId = number;

/**
 * 关联(文章、评论)Id
 */
type relatedId = string;

/**
 * 关联(文章、评论)信息
 */
type relatedInfo = string;

/**
 * 操作用户Id
 */
type operateUserId = number;

/**
 * 消息通知
 */
interface NotifyMsgDTO {}

//TODO: 111
